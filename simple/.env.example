# Jina Reader works without a key, but a key raises your rate limits.
# Keep this empty to start, add a key later.
JINA_API_KEY=

# Optional: pick a browser engine. If unset, Jina chooses a sensible default.
# Options evolve; default is fine. (You can pass per-call too.)
JINA_BROWSER=

# Optional: output directory for saved JSONL/MD
OUTPUT_DIR=data